import useStyles from './EmployeeProfileStyles';

// fixes

const EmployeeProfile = () => {
  const styles = useStyles();

  return <div style={styles.container}>profile</div>;
};

export default EmployeeProfile;
